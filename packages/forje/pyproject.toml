[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "forje"
dynamic = ["version"]
description = "The build system for your design system"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "Kipila Ltd", email = "hello@kipila.dev" }]
dependencies = ["resforge", "starlark-pyo3", "typer"]

[tool.hatch.version]
path = "src/forje/__init__.py"

[tool.hatch.envs.default]
dependencies = ["ruff", "basedpyright", "pytest"]

[project.scripts]
forje = "forje.cli.main:app"

[project.entry-points."forje.backend"]
android = "forje.backend.android:Android"

[tool.ruff]
target-version = "py312"

[tool.ruff.lint]
select = ["ALL"]
ignore = ["D100", "D104", "D105", "ANN401", "PLR2004"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.basedpyright]
include = ["src"]
typeCheckingMode = "recommended"
pythonVersion = "3.12"
